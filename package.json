{
  "name": "backpack-ui",
  "version": "5.0.0-alpha",
  "private": true,
  "description": "Monorepo for backpack-ui packages",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "precommit": "npm run test && lint-staged",
    "publish": "lerna publish --conventional-commits",
    "release": "standard-version",
    "test": "lerna run test"
  },
  "author": "Lonely Planet",
  "license": "MIT",
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.6.0",
    "lint-staged": "^6.1.0",
    "standard-version": "^4.3.0"
  },
  "lint-staged": {
    "*.js": [
      "eslint"
    ]
  }
}
